package com.mycompany.fileupload;

import org.apache.wicket.markup.html.panel.Panel;

/**
 * This panel contributes the Modal Gallery that should
 * show the uploaded files.
 *
 * TODO: finish it. Currently the gallery is not shown at all.
 */
public class FileUploadGallery extends Panel {

    public FileUploadGallery(String id) {
        super(id);
    }
}
